name: example
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  tapped_bottom_sheet:
    path: ../../tapped_bottom_sheet

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  tapped_lints:
    git:
      url: https://github.com/tappeddev/tapped_lints.git
      ref: master


flutter:
  uses-material-design: true
