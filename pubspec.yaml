name: tapped_bottom_sheet
description: "A new Flutter module project."
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  non_uniform_border: ^1.0.0
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  tapped_lints:
    git:
      url: https://github.com/tappeddev/tapped_lints.git
      ref: master

flutter:
  uses-material-design: true

  module:
    androidX: true
    androidPackage: com.example.tapped_bottom_sheet
    iosBundleIdentifier: com.example.tappedBottomSheet
